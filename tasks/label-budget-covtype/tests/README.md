# tests/

Materialize injects the code packages (`sdk`, `worlds`, `tasks_def`) and `grader.py` here at push
time (the apex/Teapot strict allowlist requires injected code under `tests/`). Nothing to edit by hand.
